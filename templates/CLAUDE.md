# <project name>

<!-- One page at most; Claude reads all of it at session start (article Play 3d).
     Run /init to draft the first three sections from your code, then cut them
     down to day-one knowledge. Add a line under "Things Claude gets wrong"
     the second time a mistake happens. -->

## Commands
- Build: <command> (must finish with "<healthy last line>")
- Test: <command> (all green)
- Lint: <command> (zero warnings)

## Conventions
<!-- The rules that matter: language and framework versions, what never to use,
     what every change must include. -->

## Architecture
<!-- Where things live and what talks to what. Name generated or frozen paths. -->

## Things Claude gets wrong
<!-- Corrections that earned their place by happening twice. -->

## Verifying your work

- Test: <command> (all pass; never skip or delete a failing test)

Run it before reporting any task complete, and paste the output.
If a test fails, fix the code, not the test.
